# plugins

Collection of plugins by matthewmorrone.

## Install

- Copy any plugin folder into your Stash plugins directory (typically `~/.stash/plugins/`).
- In Stash: Settings → Plugins → Reload Plugins.

## Plugins

### Bulk Performer Search
Clicks all performer Search buttons with delay.

### Code Editor
Replaces custom CSS/JS textareas with a fully featured code editor.

### Expand All Sources
Adds a button to expand all plugin package sources on the settings page.

### Scene Card Performers
Shows performers on scene cards and allows adding/removing performers inline.

### Scene Edit Default
Ensures the "edit" tab is selected when opening a scene when a scene is not organized. 

### Scene Edit Pinned Fields
Adds pin toggles to scene edit fields and moves pinned fields to the top.

### Scene Name Parser
Extracts performer names from scene titles and matches against StashDB.

### Scene Tagger Performer Query
Autofills Scene Tagger query inputs with concatenated performer names (lowercase).

### Scroll Restore
Restores scroll position on refresh (useful on long settings pages).

## WIPs

### Configurable Keybindings
Adds configurable keyboard shortcuts for the video player (includes frame-step via time increments).

### Hide Installed Plugins
Hides already-installed plugins from the Available Plugins list; they reappear after uninstall.

### Package ID Discourse Link
Converts `.package-id` into a link to `https://discourse.stashapp.cc/t/{id}` (opens in a new tab).

### Scene Performer Roles
Adds small role icons to performer cards on scene pages; stores neutral per-scene/per-performer roles in your browser.

### Scene Duplicate Checker Multi-File
Enhances `/sceneDuplicateChecker` by listing each scene's files inline when a scene has multiple files.
